<%= link_to "Logout", ologout_path %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <h2 class='User_Header'> <%= @current_owner %> Profile <h2>
    Owners name------------->  <%= @owner.name %><br><br>
    Owners email------------->    <%= @owner.email %><br>

      </h1>
      <div>
</div>
<div class="row">
  <% if @owner.resturants.any? %>
    <h3>resturants Available (<%= @owner.resturants.count %>)</h3>
    <ol class="microposts">
      <%= render @owner.resturants %>
    </ol>
    <%= link_to "Add products to your resturant", opods_path, class: "btn btn-danger" %>
    <% else %>
    <h3> Currently there are no resturants associated with this owner </h3>
  <% end %>
</div>
